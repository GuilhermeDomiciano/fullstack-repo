# Base URL of the Laravel backend API.
# When using the Vite dev-server proxy (the default), this can be left empty
# so that /api/* requests are proxied to http://localhost:8000.
# Override this for production builds.
VITE_API_BASE_URL=
